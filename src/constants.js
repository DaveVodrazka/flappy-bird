export const CONTAINER_ID = "container";
export const CANVAS_PROPERTIES = Object.freeze({
  WIDTH: 500,
  HEIGHT: 600,
  ID: "canvas",
});
export const GRAVITY = 0.2;
export const FLAP_SPEED = -6;
export const STARTING_OFFSET = 300;
export const PLAYER = Object.freeze({
  INITIAL_Y: 270,
  INITITAL_X: 150,
  SIZE: 15,
  COLOR: "yellow",
});
export const INTERVAL = 10;
export const PIPE = Object.freeze({
  SPEED: -3,
  HOLE_SIZE: 150,
  COLOR: "green",
  WIDTH: 100,
});
